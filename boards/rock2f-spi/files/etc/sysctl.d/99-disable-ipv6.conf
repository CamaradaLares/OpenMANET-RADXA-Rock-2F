# Disable IPv6 system-wide.
# The AIC8800 driver crashes in atomic context when IPv6 MLD multicast packets
# trigger rwnx_send_mesh_proxy_add_req. Disabling IPv6 prevents this crash.
net.ipv6.conf.all.disable_ipv6=1
net.ipv6.conf.default.disable_ipv6=1
